import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Book A Drive',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
